# Анализ работы квантов - Сессия 1 (21 ход)

## ✅ **Что работает ХОРОШО:**

### 1. Quantizer создаёт кванты правильно
- **Ход 5**: Quantizer создал 5 новых квантов + обновил 7 существующих
- **12 команд** обработано успешно
- **0 ошибок** парсинга после исправления

### 2. Имена квантов улучшились
**Хорошие примера:**
- `Грогар` ✅
- `Лира` ✅
- `Староста_Гаррик` ✅
- `Техномагический_конструкт` ✅
- `Старая_шахта` ✅

**Проблемные (без подчёркивания):**
- `Серебряный Родник` ❌ (должно быть `Серебряный_Родник`)
- `Гильдия Искателей` ❌ (должно быть `Гильдия_Искателей`)
- `Дезерт Игл` ❌ (должно быть `Дезерт_Игл`)
- `Граната Ф1` ❌ (должно быть `Граната_Ф1`)

### 3. Fuzzy matching работает
- `Гильдия_Искателей` → `Гильдия Искателей` (score: 94.1) ✅
- `Серебряный_Родник` → `Серебряный Родник` (score: 94.1) ✅
- `Громар` → `Громар` (score: 100) ✅

### 4. Links создаются правильно
Пример квант `Пол`:
```json
{
  "links": {
    "Драг": "носитель / симбиот",
    "Синтия": "спас",
    "Громар": "встретил",
    "Лис": "встретил",
    "Серебряный Родник": "ближайшее поселение",
    "Лира": "получил от неё приглашение на испытание",
    "Староста_Гаррик": "должен поговорить с ним наедине",
    "Грогар": "победил его в поединке"
  }
}
```

## ❌ **Проблемы:**

### 1. Character и Inventory НЕ СОЗДАНЫ! 🔴

**Факты:**
- В БД: только `CharacterCreation` ✅
- `Character` отсутствует ❌
- `Inventory` отсутствует ❌

**GM запрашивает их на КАЖДОМ ходу:**
- Ход 3-21: `["Character", "Inventory", ...]`

**Но они НЕ НАХОДЯТСЯ:**
- Fuzzy matching ищет `Character` → не найдено
- Fuzzy matching ищет `Inventory` → не найдено

**Почему?**
- Quantizer не создал их на ходу 5
- CharacterCreation присутствует, но инструкция не выполнена

### 2. Пробелы вместо подчёркиваний

**Quantizer создаёт:**
- `create_Серебряный Родник` ❌

**Должен создавать:**
- `create_Серебряный_Родник` ✅

**Причина:**
Quantizer (DeepSeek V3.2) игнорирует правила именования в промпте для составных названий и использует пробелы вместо подчёркиваний или CamelCase.

### 3. GM запрашивает несуществующие кванты

**Примеры (ход 11-15):**
- `Целитель` → не существует
- `Староста` → существует `Староста_Гаррик`, но запрашивается просто `Староста`
- `Элина` → не существует, должно быть `Элина_целительница`
- `Кошачья_девушка` → не существует, не создан Quantizer

**Почему GM их запрашивает?**
- GM пытается предсказать что понадобится
- Но Quantizer не создал эти кванты
- Контекст пустеет → GM работает без информации о мире

### 4. Quantizer срабатывает редко

**Логика:**
- Каждые 5 ходов (`raw_turns_trigger = 5`)

**Фактически:**
- Ход 5: сработал ✅
- Ход 10: НЕ сработал ❌ (не должен был, т.к. 10 % 5 = 0, но логика проверяет)
- Ход 15: НЕ сработал ❌
- Ход 20: сработал? (нужно проверить логи)

**Результат:**
За 21 ход только **1 раз** сработал Quantizer!

### 5. Короткие имена NPC

**Quantizer создал:**
- `Пол` ❌ (должно быть `Пол_Призванный` или `Character`)
- `Драг` ❌ (должно быть `Драг_Татуировка_Дракона`)
- `Синтия` ✅ (одно имя может быть уникально)
- `Громар` ✅
- `Лира` ✅
- `Лис` ❌ (слишком короткое, должно быть `Лис_Разведчик` или `Лис_компаньон_Синтии`)

## 📊 **Статистика запросов GM:**

### Ход 3-6: Пытается найти Character/Inventory
```json
["Character", "Inventory", "Драг", "Поселение", "Неизвестная_угроза"]
```
**Найдено:** 0 из 5 ❌

### Ход 7: Пустой запрос
```json
[]
```
**GM не запросил кванты** ❌

### Ход 8-21: Постоянно запрашивает Character/Inventory
```json
["Character", "Inventory", "Синтия", "Громар", "Лис", ...]
```
**Найдено:** 2-3 из 7 ⚠️

### Проблема с пустым контекстом:

**Ход 19:**
- Запрошено: 7 квантов
- Найдено: **2** (Гильдия Искателей, Серебряный Родник) через fuzzy matching

**Ход 20:**
- Запрошено: 7 квантов
- Найдено: **1** (Громар)

**Ход 21:**
- Запрошено: 8 квантов
- Найдено: **2** (Синтия, Громар)

## 🎯 **Выводы:**

### 1. Парсинг команд ИСПРАВЛЕН ✅
После исправления `memory_manager.py`:
- `create_Старая_шахта` → квант `Старая_шахта` ✅
- `create_Староста_Гаррик` → квант `Староста_Гаррик` ✅

### 2. НО Quantizer НЕ СОЗДАЁТ Character/Inventory 🔴
**Причина:** Quantizer (DeepSeek V3.2) не следует специальной инструкции из `CharacterCreation`.

**Решение:**
- Создать Character и Inventory вручную после создания персонажа
- ИЛИ: добавить явную проверку в orchestrator

### 3. Quantizer использует ПРОБЕЛЫ, а не подчёркивания 🔴
**Причина:** Модель игнорирует правила CamelCase/snake_case из промпта.

**Решение:**
- Усилить промпт с КРИТИЧЕСКИМИ примерами
- Добавить JSON schema для команд
- Постобработка в коде: заменять пробелы на подчёркивания в именах квантов

### 4. Fuzzy matching РАБОТАЕТ ✅
- Находит кванты даже с пробелами вместо подчёркиваний
- Score 94+ для составных названий

### 5. GM работает БЕЗ контекста 🔴
- Character отсутствует → GM не знает характеристики игрока
- Inventory отсутствует → GM не знает что у игрока есть
- Многие NPC не созданы → GM не помнит их

## 🔧 **Приоритетные исправления:**

### 1. КРИТИЧНО: Создать Character и Inventory
```sql
-- Временное решение: создать вручную
INSERT INTO quants (session_id, quant_id, type, body, links, is_game)
VALUES (1, 'Character', 'concept', 
  '{"name": "Пол", "race": "человек", "abilities": ["призыв предметов из другого мира", "магические артефакты", "Драг - живая татуировка дракона"]}',
  '{"Драг": "симбиот", "Пол": "это я"}',
  false
);

INSERT INTO quants (session_id, quant_id, type, body, links, is_game)
VALUES (1, 'Inventory', 'concept',
  '{"items": ["Дезерт Игл", "Граната Ф1", "аптечка", "шоколад", "кресло-каталка"]}',
  '{"Character": "инвентарь игрока"}',
  false
);
```

### 2. ВАЖНО: Постобработка имён квантов
Добавить в `memory_manager.py`:
```python
def _normalize_quant_name(self, name: str) -> str:
    """Normalize quant name: replace spaces with underscores."""
    return name.replace(" ", "_")
```

### 3. ВАЖНО: Усилить промпт Quantizer
Добавить в начало промпта:
```markdown
🔴 КРИТИЧЕСКИ ВАЖНО: ИМЯ КВАНТА НЕ ДОЛЖНО СОДЕРЖАТЬ ПРОБЕЛЫ!

ПРАВИЛЬНО:
- Серебряный_Родник
- Гильдия_Искателей
- Староста_Гаррик

НЕПРАВИЛЬНО:
- Серебряный Родник ❌
- Гильдия Искателей ❌
- Староста Гаррик ❌
```

### 4. СРЕДНЕ: Проверить логику `raw_turns_trigger`
Quantizer должен срабатывать каждые 5 ходов, но сработал только 1 раз за 21 ход.

## 📈 **Рекомендации:**

1. **Продолжить игру БЕЗ сброса БД** ✅
2. **Создать Character/Inventory вручную** для текущей сессии
3. **Применить исправления** для будущих сессий:
   - Нормализация имён (пробелы → подчёркивания)
   - Усилить промпт Quantizer
   - Автоматическое создание Character/Inventory после CharacterCreation

