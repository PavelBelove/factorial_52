# 🔧 Исправление формул расчёта механик

## Проблема

ГМ выдавал неправильные значения для проверок. Например:
- Карты: 10♥ + 7♣
- ГМ написал: "С **245 единицами магической силы**"
- Ожидалось: 265

## Причина

Формулы расчётов **НЕ** соответствовали правилам табletop RPG.

### ❌ Старая формула (проверки):
```python
base_roll = (card1.rank + card2.rank) * 10  # Сначала сумма, потом ×10
# 10♥ + 7♣ = (10 + 7) × 10 = 170
# + 75 (hearts) = 245 ❌
```

### ✅ Правильная формула:
```python
# Каждая карта отдельно с бонусом:
card1 = 10 × 10 + 20 (♥ совпадает с hearts) = 120
card2 = 7 × 10 + 0 (♣ не совпадает) = 70
total = 120 + 70 + 75 (stat) = 265 ✅
```

## Исправления

### 1. `calculate_check_for_pair()` - проверки вне боя
**Изменено:**
- Каждая карта теперь считается ОТДЕЛЬНО
- Бонус +20 за совпадение масти ИЛИ +10 за совпадение цвета
- Приоритет: масть > цвет

**Детали:**
```python
def calc_card_value(card: Card, check_suit: Suit) -> tuple[int, int]:
    base = card.rank * 10
    bonus = 0
    
    # +20 for suit match, OR +10 for color match (suit takes precedence)
    if card.suit == check_suit:
        bonus = 20
    elif card.color == ("red" if check_suit in [Suit.HEARTS, Suit.DIAMONDS] else "black"):
        bonus = 10
    
    return base, bonus
```

### 2. `calculate_card_bonus()` - боевые расчёты
**Изменено:**
- Убран параметр `both_same_color`
- Теперь проверяется совпадение цвета КАРТЫ и цвета ДЕЙСТВИЯ
- Каждая карта считается независимо

**Было:**
```python
if both_same_color:  # ❌ Проверка обеих карт
    return 10
```

**Стало:**
```python
action_color = "red" if action_suit in [Suit.HEARTS, Suit.DIAMONDS] else "black"
if card.color == action_color:  # ✅ Проверка карты vs действия
    return 10
```

## Примеры

### Проверка магии (hearts=75):
Карты: 10♥ + 7♣

**Старая формула:** 245  
**Новая формула:** 265 ✅

Расчёт:
- 10♥: 100 + 20 (масть) = 120
- 7♣: 70 + 0 = 70
- Итого: 120 + 70 + 75 = 265

### Атака мечом (spades=70):
Карты: K♠ + 8♣

**Расчёт:**
- K♠: 130 + 20 (масть) = 150
- 8♣: 80 + 10 (цвет) = 90
- Итого: 150 + 90 + 70 = 310

### Защита щитом (diamonds=35):
Карты: 6♦ + 5♥

**Расчёт:**
- 6♦: 60 + 20 (масть) = 80
- 5♥: 50 + 10 (цвет) = 60
- Итого: 80 + 60 + 35 = 175

## Тестирование

После перезапуска системы проверить:
1. ГМ показывает правильные значения
2. Бонусы применяются корректно
3. Боевые расчёты работают по правилам

---

*Исправлено: 2026-01-18*

